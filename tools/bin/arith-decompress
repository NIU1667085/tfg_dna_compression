#!/usr/bin/env bash
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SRC_DIR="$SCRIPT_DIR/../src/Reference-arithmetic-coding/python"

exec python3 "$SRC_DIR/adaptive-arithmetic-decompress.py" "$@"
